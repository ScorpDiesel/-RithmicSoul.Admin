@using RithmicSoul.Models.Survey.Dtos;

<MudDialog>
    <DialogContent>
        <MudForm>
            <MudTextField T="int" Label="@nameof(_dto.QuestionTypeId)" Class="mb-8" @bind-Value="@_dto.QuestionTypeId" Required></MudTextField>
            <MudTextField T="int" Label="@nameof(_dto.RatingTypeId)" Class="mb-8" @bind-Value="@_dto.RatingTypeId" Required></MudTextField>
        </MudForm>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Cancel</MudButton>
        <MudButton Color="Color.Primary" OnClick="Save">Save</MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] 
    MudDialogInstance MudDialog { get; set; }

    private RatingTypeQuestionTypeDto _dto = new();

    void Save()
    {
        MudDialog.Close(DialogResult.Ok(_dto));
    }

    void Cancel() => MudDialog.Cancel();
}