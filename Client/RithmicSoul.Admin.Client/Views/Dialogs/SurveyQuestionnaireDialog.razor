@using RithmicSoul.Models.Survey.Dtos;

<MudDialog>
    <DialogContent>
        <MudForm>
            <MudTextField T="int" Label="@nameof(Model.QuestionId)" Class="mb-8" @bind-Value="@Model.QuestionId" Required></MudTextField>
            <MudTextField T="int" Label="@nameof(Model.SurveyId)" Class="mb-8" @bind-Value="@Model.SurveyId" Required></MudTextField>
        </MudForm>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Cancel</MudButton>
        <MudButton Color="Color.Primary" OnClick="Save">Save</MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] 
    MudDialogInstance MudDialog { get; set; }

    [Parameter]
    public SurveyQuestionnaireDto? Model { get; set; }

    protected override void OnInitialized()
    {
        Model ??= new SurveyQuestionnaireDto();
    }

    void Save()
    {
        MudDialog.Close(DialogResult.Ok(Model));
    }

    void Cancel() => MudDialog.Cancel();
}