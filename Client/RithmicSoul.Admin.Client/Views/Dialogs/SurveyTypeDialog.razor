@using RithmicSoul.Models.Survey.Dtos;

<MudDialog>
    <DialogContent>
        <MudForm>
            <MudTextField T="string" Label="@nameof(Model.SurveyTypeName)" Class="mb-8" @bind-Value="@Model.SurveyTypeName" Required></MudTextField>
        </MudForm>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Cancel</MudButton>
        <MudButton Color="Color.Primary" OnClick="Save">Save</MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] 
    MudDialogInstance MudDialog { get; set; }

    [Parameter]
    public SurveyTypeDto? Model { get; set; }

    protected override void OnInitialized()
    {
        Model ??= new SurveyTypeDto();
    }

    void Save()
    {
        MudDialog.Close(DialogResult.Ok(Model));
    }

    void Cancel() => MudDialog.Cancel();
}